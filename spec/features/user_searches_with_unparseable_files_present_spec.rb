require 'spec_helper'
require 'tempfile'

describe 'Searching for files with a single regex' do
  context 'both parseable and unparseable files are present' do
    it 'returns filenames of the files where all regexes matched'

    it 'prints warnings for each unparseable file to STDERR'
  end
end
